<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="4dp"
    android:id="@+id/item_list_layout">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/item_makanan_foto"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@android:color/darker_gray" />

    <TextView
        android:id="@+id/item_makanan_nama"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:maxLines="2"
        app:fontFamily="@font/poppins"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/item_makanan_foto"
        app:layout_constraintTop_toTopOf="@+id/item_makanan_foto"
        tools:text="Soekarno" />

    <TextView
        android:id="@+id/item_makanan_desc"
        android:layout_width="0dp"
        android:layout_height="35dp"
        android:textSize="12sp"
        android:maxLines="2"
        app:fontFamily="@font/poppins"
        app:layout_constraintEnd_toEndOf="@+id/item_makanan_nama"
        app:layout_constraintStart_toStartOf="@+id/item_makanan_nama"
        app:layout_constraintTop_toBottomOf="@+id/item_makanan_nama"
        tools:text="Presiden Pertama RI" />
</android.support.constraint.ConstraintLayout>